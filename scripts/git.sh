#!/bin/env bash

#git setup

echo "Setting up git"
git config --global user.name "WALLFLOWERYWOLF"
git config --global user.email "121654472+WALLFLOWERYWOLF@users.noreply.github.com"
git config --global init.defaultBranch main
git config --global color.ui auto
git config --global pull.rebase false
git config --global core.editor "nvim -f"
